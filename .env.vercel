# Vercel Environment Variables Template
# Copy these to your Vercel dashboard environment variables

# Flask Configuration
FLASK_ENV=production
FLASK_DEBUG=false
SECRET_KEY=your-secret-key-here

# Admin Configuration
ADMIN_USERNAME=admin
ADMIN_PASSWORD_HASH=your-generated-password-hash
ADMIN_SESSION_TIMEOUT=3600

# File Upload Settings
MAX_FILE_SIZE=104857600
ALLOWED_EXTENSIONS=pdf,txt,jpg,jpeg,png,gif,doc,docx,xls,xlsx,csv,zip,rar

# Storage Configuration (REQUIRED for Vercel - use S3, GCS, or Azure)
STORAGE_TYPE=s3

# AWS S3 Configuration (if using S3)
AWS_ACCESS_KEY_ID=your-aws-access-key
AWS_SECRET_ACCESS_KEY=your-aws-secret-key
AWS_REGION=us-east-1
S3_BUCKET_NAME=your-bucket-name

# Google Cloud Storage (if using GCS)
# GCS_BUCKET_NAME=your-gcs-bucket
# GOOGLE_APPLICATION_CREDENTIALS=path-to-service-account.json

# Azure Blob Storage (if using Azure)
# AZURE_STORAGE_CONNECTION_STRING=your-connection-string
# AZURE_CONTAINER_NAME=your-container-name

# CORS Configuration
CORS_ORIGINS=https://your-frontend-domain.netlify.app,https://ncryp.netlify.app

# Firebase Configuration (optional)
FIREBASE_CREDENTIALS_PATH=firebase-credentials.json

# Vercel-specific
VERCEL=1
